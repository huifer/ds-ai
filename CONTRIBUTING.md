# 贡献指南

感谢你考虑为 **ds-ai** 做出贡献！🎉

本项目欢迎任何形式的贡献，无论你是修改一个错别字、补充一个示例，还是提出全新章节。

---

## 🤝 贡献方式

### 1. 报告问题（Issues）

发现错误、文档不清晰、内容缺失？欢迎提 Issue。请尽量包含：

- 章节与文件路径
- 期望内容 vs 实际内容
- 复现步骤（如果是代码示例）
- 截图（如适用）

### 2. 改进文档（Pull Requests）

- 修正错别字、表述不清之处
- 补充图示、表格、公式推导
- 翻译为英文 / 其它语言

### 3. 提交示例代码

- 在 `examples/<chapter>/` 下添加新的最小可运行示例
- 保证代码可在 `numpy` + 标准库下运行（深度学习示例允许 `torch`）
- 提供简短 README 说明运行方式与预期输出

### 4. 提出新章节

如果你想添加新的章节，请先开 Issue 讨论：

- 该章节覆盖哪些数据结构？
- 对应哪些 AI 场景？
- 预估篇幅与所需图示？

达成共识后再开始撰写，避免重复劳动。

---

## 📋 提交规范

### 分支策略

- `main`：稳定分支，受保护
- `feat/<topic>`：新章节/新专题
- `fix/<topic>`：修正错误
- `docs/<topic>`：纯文档改动

### Commit 消息

推荐使用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)：

```
feat: 增加第 5 章 - 树与决策树示例
fix: 修正第 3 章堆章节中的复杂度错误
docs: 完善 README 快速开始部分
style: 统一代码块语言标识
refactor: 重构 examples/ 目录结构
```

### PR 流程

1. Fork 仓库并创建分支
2. 完成修改，确保本地构建/示例可运行
3. 在 PR 描述中写明：
   - 改动动机
   - 主要修改点
   - 测试方式（如适用）
   - 相关 Issue 编号
4. 等待 Review，根据反馈修改
5. 通过 CI 与 Review 后合并

---

## ✍️ 写作风格指南

为了保持文档一致性，请遵守以下约定：

### 语言

- **主体中文**，关键术语首次出现时给出英文
  - ✅ "向量数据库（Vector Database）"
  - ❌ "vector database"
- 代码注释优先英文；解释性段落用中文

### 章节结构

每个章节建议包含：

```markdown
# 第 N 章 [数据结构名] ([English Name])

> 一句话定义本章要解决的问题

## 是什么
## 为什么（AI 中的角色）
## 怎么用（最小示例）
## 复杂度与权衡
## 延伸阅读
```

### 代码示例规范

- ✅ 包含 `if __name__ == "__main__":` 入口
- ✅ 中文/英文双语注释（关键步骤）
- ✅ 单一职责，每个示例只演示一个点
- ❌ 不要在示例里写大段业务逻辑
- ❌ 不要硬编码绝对路径

### 图示规范

- 优先 SVG / Mermaid（可版本化）
- PNG 必须放进 `assets/` 并附 `assets/SOURCES.md` 说明来源
- 复杂图建议提供原始 `.drawio` / `.mmd` 文件

---

## 🧪 测试与质量

虽然这是文档项目，但仍希望：

- 所有代码示例可运行（至少在你描述的环境下）
- Markdown 链接不出现 404
- 引用外部资料时附 URL + 访问日期

我们会在 CI 中跑：

- Markdown 链接检查
- Python 示例语法校验（`python -m py_compile`）
- 中英文拼写基本检查

---

## 📜 行为准则

请阅读并遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。

简而言之：**友善、尊重、就事论事**。

---

## ❓ 提问

不确定从哪开始？

- 看 [Issues](https://github.com/huifer/ds-ai/issues) 里的 `good first issue` 标签
- 在 Discussions 里发起提问
- 直接联系 Maintainer

期待你的 PR！🙌