# 安全政策

## 报告漏洞

请**不要**通过公开 Issue 报告安全问题。

通过以下方式私下联系维护者：

- GitHub: [@huifer](https://github.com/huifer)
- 或在 Issue 中以 `@huifer` 提及并明确表示"请私下沟通"

请在报告中包含：

- 问题描述
- 复现步骤 / PoC
- 受影响的文件 / 路径
- 潜在影响
- 您的环境（OS / Node 版本等）

我们会在 7 个工作日内回复。

## 披露原则

- 我们遵循**负责任披露（Coordinated Disclosure）**原则
- 在漏洞修复前，请勿公开细节
- 修复后我们会在 CHANGELOG 中致谢报告者（如报告者同意）

## 已知安全设计

本项目在设计层面已考虑以下安全要素：

- **Discord Token** 存于 `.env`，权限 `600`
- **白名单** `ALLOWED_USER_IDS`，默认仅限本人（在 `.env` 中配置）
- **`write_file` 路径白名单**：仅允许写入项目内 `data/` 子目录，绝对路径和 `..` 都拒绝
- **Scheduler tick = 30s**，错过任务最多 1 小时内补跑（避免重启时把陈年任务全补跑）
- **Pi Agent RPC 子进程隔离**：不直接暴露 daemon 权限给 Pi 子进程

## 适用范围

本安全政策**不涵盖**以下场景：

- 用户基于本项目代码自行部署的实例
- 用户参考本项目文档配置的服务
- 第三方依赖（discord.js、Pi Agent 等）的安全问题，请前往对应仓库报告