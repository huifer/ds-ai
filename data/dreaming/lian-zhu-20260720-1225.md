# 莲·珠 联想碎片

> 触发:用户主动要求从记忆库找最近灵感
> 时间:2026-07-20 12:25
> 来源:今日(2026-07-20) `data/dreams/artifacts/lian-zhu/xiasi-2026-07-20-lian-zhu-fw3kl5-*.md` 三条产物

---

## 灵感 1:tag-bypass-search(总分 0.562 · helpful)
- **来源**: `mem_1784465063423_vague-idea` + `mem_1784465063453_no-tags` + `mem_1784465063277_feature-idea`
- **联想**: 一边说"懒得打标签",一边悄悄建 `/search` 走全文检索—— tag 是 HTTPS, search 是给 localhost 留的 raw 入口。"无家可归的记忆"靠搜索逃生梯回家。
- **行动**: 优先建设低摩擦的全文检索通道,**别再执着于补强 tag 体系**;新记忆如果 5 秒内想不出合适 tag,直接进 "untagged" 索引,等搜索召回即可。

## 灵感 2:ctx-trauma(总分 0.512 · harmful ⚠️)
- **来源**: `mem_1784465063384_use-javascript` + `mem_1784460016137_prefer-typescript` + `mem_1784458406997_rpc-vs-tui`
- **联想**: JS/TS 偏好反复横跳,选 RPC 不是因为 RPC 完美,而是受不了 TUI 的 ctx stale。两条偏好的"创伤"等价。
- **行动**: 影子判决有害,因果证据不足,容易心理化跑偏——**记下现象即可,不要据此改架构决策**。需要的是"既要快又要稳"的混合体(本地偏好用 TS 写类型边界,运行时走 RPC)。

## 灵感 3:trust-stratification(总分 0.585 · helpful)
- **来源**: `mem_1784459674022_production-https` + `mem_1784460641794_allow-http` + `mem_1784458407023_error-handling-must-log`
- **联想**: 三条 constraint 不是矛盾,是分层——传输层加密(HTTPS)、运行时容错(允许内网 HTTP)、错误层考古(必须留日志)。他真正拒绝的不是 HTTP,是 **silent failure**。
- **行动**: 抽出统一原则 "**拒绝静默失败**":
  - 对外:加密传输不可妥协
  - 内网:速度优先,但失败必须可见
  - 任意层 panic / 错误 → 强制留痕,可观测

---

## 三条连起来的元洞察

今天三条产物放一起,其实都在讲**"对失败的拒绝"**:
- 灵感 1 拒绝"标签系统找不到记忆"的失败
- 灵感 2 拒绝"运行时上下文腐烂"的失败
- 灵感 3 拒绝"错误悄悄溜走"的失败

→ 元主题:**老张的工程观 = 把所有 silent failure 变成 loud failure**,无论发生在检索层、运行时层、还是传输层。

---

## 落盘与下一步

- 本文件路径:`data/dreaming/lian-zhu-20260720-1225.md`
- `helpful` 的 2 条可喂给 `gui-cang` 做归藏(沉淀为工程原则)
- `harmful` 的 1 条仅做记录,不入库
- 下次触发 `ming-tai`(铭台)时,把元主题"拒绝静默失败"提为一条可引用的台训